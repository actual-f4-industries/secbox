<Application x:Class="Secbox.Sentinel.AlertUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnLastWindowClose">
  <Application.Resources>
    <!--
      Dark theme + critical-red accent. Solid colors only — no
      gradients / images — so the exe starts fast and renders the
      same on every Windows version we care about.
    -->
    <SolidColorBrush x:Key="Background"  Color="#1c1f24" />
    <SolidColorBrush x:Key="Surface"     Color="#262a31" />
    <SolidColorBrush x:Key="Border"      Color="#3a3f4a" />
    <SolidColorBrush x:Key="Text"        Color="#e8eaed" />
    <SolidColorBrush x:Key="TextDim"     Color="#9aa0a6" />
    <SolidColorBrush x:Key="Danger"      Color="#8b1a1a" />
    <SolidColorBrush x:Key="DangerLight" Color="#f6c6c6" />
    <SolidColorBrush x:Key="Accent"      Color="#5e89f0" />

    <Style TargetType="Button">
      <Setter Property="Background" Value="{StaticResource Surface}" />
      <Setter Property="Foreground" Value="{StaticResource Text}" />
      <Setter Property="BorderBrush" Value="{StaticResource Border}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="14,6" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="{StaticResource Danger}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderBrush" Value="{StaticResource Danger}" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
  </Application.Resources>
</Application>
